var sinon = require('sinon');

var mongoose = require('mongoose');
require('sinon-mongoose');


 // Test will pass if the poll is saved
describe("DAO Post a new poll", function(){
    xit("should create new post", function(done){
        
    });
    // Test will pass if the poll is not saved
    xit("should return error, if poll not saved", function(done){
        
    });
});